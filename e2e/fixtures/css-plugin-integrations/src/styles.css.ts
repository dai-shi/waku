import { style } from '@vanilla-extract/css';

export const vanillaServerStyle = style({
  color: 'rgb(0, 128, 0)',
});

export const vanillaClientStyle = style({
  backgroundColor: 'rgb(255, 255, 0)',
});
